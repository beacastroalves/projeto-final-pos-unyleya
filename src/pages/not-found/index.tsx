import UserTemplate from "../../templates/user-template"

const NotFound = () => {
  return (
    <UserTemplate>
      <h1 className="text-center w-screen text-[32px] font-bold mt-20">Página não encontrada...</h1>
    </UserTemplate>
  )
};

export default NotFound;